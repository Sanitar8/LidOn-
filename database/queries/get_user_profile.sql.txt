SELECT u.id, u.email, u.first_name, u.middle_name, u.last_name, u.city_id,
       c.name AS city_name,
       c.region AS city_region
FROM users u
LEFT JOIN cities c ON c.id = u.city_id
WHERE u.id = ?